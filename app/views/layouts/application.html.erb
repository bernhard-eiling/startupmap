<!DOCTYPE html>
<html>
<head>
  <title>Startupmap</title>
  <script src="https://maps.googleapis.com/maps/api/js?v=3.exp&sensor=false"></script>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>

  <%= csrf_meta_tags %>
</head>
<body>
<div id="user_nav">
    <%= link_to "Home", root_path %>
  
  <% if current_user %>
    Logged in as <%= current_user.email %> | <%= current_user.name %> |
    <%= link_to "Watch your Profile", user_path(current_user.id) %> |
    <%= link_to "Log out", destroy_user_session_path, :method => :delete  %>
  <% else %>
    <%= link_to "Sign up via Facepalm", user_omniauth_authorize_path(:facebook) %> |
    <%= link_to "Sign up", new_user_registration_path %> |
    <%= link_to "log in", new_user_session_path %>
  <% end %>
</div>

<div id="left-wrapper">
  <div id="logo"></div>
  <div id="kind">
    <div id="startup" class="clickable replace-kind" value="startup">Startup
      <div class="counter"><%= @numStartups.length %></div>
    </div>
    <div id="investor" class="clickable replace-kind" value="investor">Investor
      <div class="counter"><%= @numInvestors.length %></div>
    </div>
  </div>
  <div class="divider">-------------------</div>
  <div id="city">
    <div id="graz" class="clickable replace-city" value="Graz">Graz
      <div class="counter"><%= @numGraz.length %></div>
    </div>
    <div id="salzburg" class="clickable replace-city" value="Salzburg">Salzburg
      <div class="counter"><%= @numSalzburg.length %></div>
    </div>
    <div id="wien" class="clickable replace-city" name="wien" value="Wien">Wien
      <div class="counter"><%= @numWien.length %></div>
    </div>
  </div>
</div>

<div id="right-wrapper">
  <div id="search">
    <input id="search-field" type="text" name="fname">
  </div>
  <div id="search-result"></div>
</div>


<%= link_to "View all Users", users_path %>


<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>



<%= yield %>

</body>
</html>
